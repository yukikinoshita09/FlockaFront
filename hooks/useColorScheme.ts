// Temporarily force light mode across the app.
// Revert this file to use system color scheme when you want to re-enable dark mode.
export function useColorScheme(): 'light' | 'dark' {
  return 'light';
}
